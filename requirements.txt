# =============================================================================
# LeechBot - Advanced Telegram File Transloader
# =============================================================================
# Project   : LeechBot
# Developer : Shinei Nouzen
# GitHub    : https://github.com/Shineii86
# Telegram  : https://telegram.me/Shineii86
# =============================================================================

# Core framework
kurigram>=2.2.23
tgcrypto>=1.2.5

# Configuration
python-dotenv>=1.2.2

# RSS parsing
feedparser>=6.0.12

# Downloaders
yt-dlp>=2026.6.9
curl_cffi>=0.15.0
bgutil-ytdlp-pot-provider>=1.3.1
gallery-dl
aiohttp>=3.14.1

# Google Drive (optional — mount drive and set token.pickle path)
google-api-python-client>=2.198.0
google-auth-httplib2>=0.4.0
google-auth-oauthlib>=1.4.0

# Video processing
moviepy>=2.2.1

# Image processing
Pillow>=12.3.0

# System monitoring & utilities
psutil>=7.2.2
GPUtil>=1.4.0
natsort>=8.4.0
pytz>=2026.2

# Torrent/Magnet downloads (install via system package manager — not on PyPI)
# Debian/Ubuntu: sudo apt install python3-libtorrent
# Conda:         conda install -c conda-forge libtorrent
# The torrent module gracefully skips if libtorrent is unavailable.

# Optional system dependencies (install via package manager):
#   aria2        — HTTP/FTP/Bittorrent downloader
#   ffmpeg       — video/audio processing
#   7zip / p7zip — archive handling
#   unrar        — RAR archive extraction
#   megatools    — Mega.nz downloads
